/**
 * 参加者編集メニュー — 参加者 URL と運営画面の切り分け（単一の正）
 *
 * ## 参加者 URL（`app/player/[id]/page.tsx`）
 * - 運営向け「参加者編集メニュー」（無効化・削除・特別属性の設定を選ぶモーダル）を**表示しない**。
 * - 無効化・完全削除・特別属性の設定は行わない（閲覧・通常操作のみ）。
 *
 * ## 運営ホーム（`components/PokabuAdminUI.tsx` / `app/page.tsx`）
 * - 参加者一覧の各行「編集」から**参加者編集メニューを表示する**。
 * - 開ける一覧は次の**のみ**（Good ランキング等からは開かない）:
 *   - 交流会モード: 「待機中」人数をタップしたシート内の待機／休憩リスト
 *   - 卓一覧モード: 階級カードを展開したときの参加者一覧
 *
 * 実装・コメントを変えるときは本ファイルと両画面のルールをずらさないこと。
 *
 * 参照: `app/player/[id]/page.tsx`（交流会 UI ルール）、`components/PokabuAdminUI.tsx`（`canOpenParticipantEditMenu` 等）。
 */

export {};
